# MongoDB connection (data-access)
DB_USERNAME=
DB_PASSWORD=
DB_APP_NAME=
DB_HOST=
DB_NAME=

# Frontend GraphQL endpoint
# Use /graphql when both frontend and backend are served from the same Vercel deployment.
VITE_GRAPHQL_URL=/graphql
